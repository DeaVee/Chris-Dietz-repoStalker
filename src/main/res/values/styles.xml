<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="Octo.Widget.ErrorMsg" parent="TextAppearance.AppCompat.Headline">
        <item name="android:gravity">center_horizontal</item>
    </style>

    <style name="Octo.Widget.UserAvatar" parent="">
        <item name="android:layout_width">@dimen/avatar_width</item>
        <item name="android:layout_height">@dimen/avatar_height</item>
    </style>

    <style name="Octo.Widget.UserName" parent="">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/name_text_size</item>
    </style>

</resources>
